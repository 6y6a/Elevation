zoom = new WOW(
    {
        boxClass:     'animation',      // default
        animateClass: 'animated zoomIn', // default
        offset:       300,          // default
        mobile:       true,       // default
        live:         true        // default
    }
);

zoom.init();